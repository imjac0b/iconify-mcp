{
  "name": "iconify-mcp",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "mkdir -p bin && bun build src/index.ts --compile --minify --sourcemap --outfile bin/iconify-mcp"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.18.2",
    "ofetch": "^1.4.1",
    "zod": "^3.25.76"
  }
}
